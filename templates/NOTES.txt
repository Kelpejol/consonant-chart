✅ Consonant Relayer installed successfully!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 INSTALLATION SUMMARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Release:          {{ .Release.Name }}
Namespace:        {{ include "consonant-relayer.namespace" . }}
Chart:            {{ .Chart.Name }}-{{ .Chart.Version }}
App Version:      {{ .Chart.AppVersion }}

Cluster:          {{ .Values.cluster.name }}
{{- if .Values.cluster.region }}
Region:           {{ .Values.cluster.region }}
{{- end }}
{{- if .Values.cluster.environment }}
Environment:      {{ .Values.cluster.environment }}
{{- end }}

Backend:          {{ .Values.backend.url }}
gRPC Endpoint:    {{ include "consonant-relayer.grpcEndpoint" . }}
OTEL Collector:   {{ if .Values.otelCollector.enabled }}✓ Enabled ({{ .Values.otelCollector.type }}){{ else }}✗ Disabled{{ end }}
Network Policy:   {{ if .Values.networkPolicy.enabled }}✓ Enabled{{ else }}✗ Disabled{{ end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 VERIFY INSTALLATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Check relayer pod:
   kubectl get pods -n {{ include "consonant-relayer.namespace" . }} -l app.kubernetes.io/component=relayer

2. Check OTEL collector:
   kubectl get pods -n {{ include "consonant-relayer.namespace" . }} -l app.kubernetes.io/component=otel-collector

3. View relayer logs:
   kubectl logs -n {{ include "consonant-relayer.namespace" . }} -l app.kubernetes.io/component=relayer -f

4. Check gRPC connection:
   kubectl port-forward -n {{ include "consonant-relayer.namespace" . }} svc/{{ include "consonant-relayer.fullname" . }} 8080:8080
   curl http://localhost:8080/health

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📚 NEXT STEPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Verify cluster appears in backend UI
2. Install KAgent (if not already installed)
3. Deploy agents and start using Consonant

For support: support@consonant.xyz
Documentation: https://docs.consonant.xyz